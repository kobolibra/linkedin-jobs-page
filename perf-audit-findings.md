# 性能审计阶段记录

日期：2026-08-16

## 当前基线

桌面端首次加载后：`jobs.json` 约 2.32 MB，DOM 约 76,312 个节点，职位卡片 4,511 个，日期分组 46 个，SVG 约 9,036 个，其中 Top 30 气泡 30 个。脚本和样式资源均在约 7–52 KB 范围内，最大资源是 jobs.json。

Top 30 四视图同步切换耗时约 3.7–10.9 ms，未发现明显的切换卡顿。

## 已实施优化

职位卡片原先为每张卡片分别绑定收藏、屏蔽、已读和机构点击/键盘事件；当前已改为 `#jobs` 容器上的 click/keydown 事件委托，减少数千个独立监听器带来的内存和绑定开销。交互回归确认：直接触发机构名称点击后，机构筛选值由 `all` 正确变为 `State Street`。

## 移动端检查

使用 390×844 视口生成移动端截图。首屏标题、统计带、地区分布、近 30 日新增和 Top 30 四视图控件均在窄屏内正常排列，没有发现横向溢出；Top 30 在移动端继续缩放显示。

## 初步瓶颈判断

主要成本来自 4,500+ 职位卡片的 DOM 数量、9,000+ SVG/图形节点以及 2.32 MB 数据文件。Top 30 重绘和脚本资源不是主要瓶颈。后续应优先验证是否可安全采用列表虚拟化或分段渲染；若不改动结构，当前事件委托是低风险收益最高的优化。固定噪声滤镜和入场动画属于次要移动端 GPU 成本，需要在视觉不变的前提下谨慎处理。

## 交互状态

Top 30 ALL/CN/HK/SG 四视图均已点击验证并能重新计算公司集合与统计值。Company Insight 机构筛选事件委托已验证。

## 最终回归

`perf-audit-49` 桌面端重新加载正常，首屏资源与图表完整，Top 30 四视图控件仍存在，职位卡片事件委托可用。移动端 390×844 最终截图正常：标题、统计区、地区分布、近 30 日新增和 Top 30 控件均无横向溢出，Top 30 图表在首屏下方正常开始渲染。

新增 `.day { content-visibility:auto; contain-intrinsic-size:0 420px; }`，移动端使用 360px intrinsic size；该规则只延迟屏外日期分组绘制，不删除数据，不改变 DOM 语义和滚动内容。
